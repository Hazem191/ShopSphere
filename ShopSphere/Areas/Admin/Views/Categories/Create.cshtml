@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model Category
@{
    ViewData["Title"] = Localizer["إضافة فئة جديدة"];
}

<div class="row justify-content-center mt-4">
    <div class="col-md-6">
        <div class="card shadow-sm border-0" style="border-radius:15px;">
            <div class="card-header fw-bold bg-white border-bottom-0 pt-4 px-4">
                <h4 class="mb-0"><i class="fas fa-plus-circle me-2 text-primary"></i> @Localizer["إضافة فئة جديدة"]</h4>
            </div>
            <div class="card-body p-4">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div class="mb-4">
                        <label asp-for="Name" class="form-label fw-semibold">@Localizer["اسم الفئة"]</label>
                        <input asp-for="Name" class="form-control rounded-pill px-3" placeholder='@Localizer["مثال: إلكترونيات..."]' />
                        <span asp-validation-for="Name" class="text-danger small"></span>
                    </div>
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary rounded-pill px-4">
                            <i class="fas fa-save me-1"></i> @Localizer["حفظ"]
                        </button>
                        <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">@Localizer["إلغاء"]</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
